# SnapBack VSCode Extension - Environment Configuration Template
#
# Copy to .env.local, .env.staging, or .env.production
# For development: cp .env.example .env.local
#
# This configuration is for the VSCode extension development and testing
#
# IMPORTANT: Never commit .env.local, .env.staging, .env.production
# Add to .gitignore: .env.local, .env.staging, .env.production

# ================================
# Environment Identification
# ================================
# Options: development | staging | production
NODE_ENV=development

# ================================
# API Configuration
# ================================
# SnapBack API server URL (where extension connects to)
# Development: http://localhost:3001
# Staging: https://api.staging.snapback.dev
# Production: https://api.snapback.dev
API_URL=http://localhost:3001

# ================================
# OAuth Configuration
# ================================
# OAuth redirect URL (must match registered URL in GitHub/Google OAuth app)
# Development: vscode://snapback.snapback/auth-callback
# Staging: vscode://snapback.snapback/auth-callback
# Production: vscode://snapback.snapback/auth-callback
OAUTH_REDIRECT_URI=vscode://snapback.snapback/auth-callback

# OAuth scopes (GitHub)
# Default: user, repo, read:user, user:email
OAUTH_SCOPES=user,repo,read:user,user:email

# ================================
# Authentication
# ================================
# Token endpoint for obtaining extension tokens
TOKEN_ENDPOINT=http://localhost:3001/auth/token

# Token refresh endpoint
REFRESH_TOKEN_ENDPOINT=http://localhost:3001/auth/token/refresh

# ================================
# Extension Configuration
# ================================
# Extension ID (should match package.json)
EXTENSION_ID=snapback.snapback

# Extension name
EXTENSION_NAME=SnapBack

# ================================
# Logging
# ================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Enable VSCode output channel logging
VSCODE_LOG=true

# ================================
# Feature Flags
# ================================
# Enable auto-protection feature
ENABLE_AUTO_PROTECT=true

# Enable snapshot management UI
ENABLE_SNAPSHOT_UI=true

# Enable analytics
ENABLE_ANALYTICS=true

# ================================
# Development/Testing Only
# ================================
# Enable VSCode extension development host debugging
# Used when running Extension Development Host
EXTENSION_DEV_MODE=false

# Mock authentication (for testing without real OAuth)
MOCK_AUTH=false

# Use local test workspace
USE_TEST_WORKSPACE=false

# Enable debug logging in extension
EXTENSION_DEBUG=false
