/**
 * Session Types - Re-exports from SDK
 *
 * This module re-exports session types from the SDK for backward compatibility
 * with existing VSCode extension code.
 */

export type {
	SessionCandidate,
	SessionFileEntry,
	SessionFinalizeReason,
	SessionId,
	SessionManifest,
} from "@snapback/sdk";
