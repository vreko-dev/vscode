/**
 * VSCode Path Normalizer - Re-exports from SDK
 *
 * This module re-exports platform-agnostic path normalization utilities
 * from @snapback/sdk for backward compatibility within the VSCode extension.
 *
 * @module PathNormalizer
 */

export { areEqual, getDepth, isWithin, normalize } from "@snapback/sdk";
